# GateMockAI v2.1 — Page Prep & Diagram Crop Service
# OCR dependencies (paddlepaddle, paddleocr, pymupdf) REMOVED — Gemini reads PDF directly

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# Image processing (crop only — no OCR)
Pillow>=10.4.0
pdf2image>=1.17.0

# Storage
minio>=7.2.8

# Utilities
python-multipart>=0.0.9
pydantic>=2.8.2
python-dotenv>=1.0.1
httpx>=0.27.0
