# Core FastAPI framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# PDF processing
pymupdf>=1.24.10          # Digital PDF extraction (fitz)

# OCR for scanned PDFs
paddlepaddle>=2.6.2      # PaddlePaddle CPU backend (supports arm64/Apple Silicon)
paddleocr>=2.8.1         # PaddleOCR with layout analysis

# Image processing
Pillow>=10.4.0
opencv-python-headless>=4.10.0.84
numpy>=1.26.4

# MinIO object storage client
minio>=7.2.8

# HTTP client (for health checks)
httpx>=0.27.0

# Data validation
pydantic>=2.8.2

# Utilities
python-multipart>=0.0.9  # For FastAPI file upload support
python-dotenv>=1.0.1
